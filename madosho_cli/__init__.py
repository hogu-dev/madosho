"""madosho CLI - a thin HTTP client over madosho's API.

Human-runnable for testing and machine-drivable by a research agent (every
subcommand takes --json). Speaks only HTTP (no madosho/madosho_server imports),
so it is a pure client and could be lifted into its own repo unchanged.
"""
